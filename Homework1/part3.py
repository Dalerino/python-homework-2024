# Ask the user to input a number, which will be converted to integer, float, and string numbers
num=input("Enter a number: ")
int=int(num)
float=float(num)
str=str(num)

print(f"Integer = {int}")
print(f"Float = {float}")
print(f"String = '{str}'")